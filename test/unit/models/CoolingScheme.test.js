'use strict'
/* global describe, it */

const assert = require('assert')

describe('CoolingScheme Model', () => {
  it('should exist', () => {
    assert(global.app.api.models['CoolingScheme'])
  })
})
